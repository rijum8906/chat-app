import { Typography } from '@/components/ui/typography';
import { Button } from '@/components/ui/button';

const HomePage = () => {
  return (
    <>
      <Typography variant="h1">This is a simple Home Page For testing</Typography>
      <Button variant="ghost">A sample button</Button>
    </>
  );
};

export default HomePage;
